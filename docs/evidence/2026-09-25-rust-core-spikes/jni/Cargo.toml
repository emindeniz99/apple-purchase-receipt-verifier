[package]
name = "aprv-jni"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
aprv = { package = "apple-purchase-receipt-verifier", path = "../../../../rust" }
jni = "0.21"
