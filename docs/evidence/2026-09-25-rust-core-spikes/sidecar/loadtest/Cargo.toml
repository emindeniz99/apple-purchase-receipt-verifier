[package]
name = "loadtest"
version = "0.1.0"
edition = "2021"
[dependencies]
aprv = { package = "apple-purchase-receipt-verifier", path = "$REPO/rust" }
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
