package applereceipt

// go/roots/certs is a generated copy of the repo-root certs/ directory,
// because go:embed cannot reach outside the module directory. Run
// `go generate ./...` after any change to certs/; CI runs the same command
// and fails on a diff, so the copy cannot silently go stale.
//
//go:generate go run ./internal/gencerts
